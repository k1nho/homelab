apiVersion: v1
kind: Service
metadata:
  name: koffan-svc
  labels:
    app.kubernetes.io/name: koffan
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: koffan
  ports:
    - port: 80
      targetPort: 8080
