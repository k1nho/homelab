apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: koffan-ingress
spec:
  defaultBackend:
    service:
      name: koffan-svc
      port:
        number: 80
  ingressClassName: tailscale
  tls:
    - hosts:
        - koffan
