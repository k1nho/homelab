---
- hosts: k3s_server
  become: true
  tasks:
    - include_tasks: ../tasks/server.yaml

- hosts: k3s_workers
  become: true
  tasks:
    - include_tasks: ../tasks/workers.yaml

- hosts: k3s_server
  become: true
  tasks:
    - include_tasks: ../tasks/helm.yaml
    - include_tasks: ../tasks/cilium.yaml
    - include_tasks: ../tasks/kubeconfig.yaml
