apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: opencost-ingress
spec:
  defaultBackend:
    service:
      name: opencost-homelab
      port:
        number: 9090
  ingressClassName: tailscale
  tls:
    - hosts:
        - opencost
